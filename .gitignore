# o log e local a cada maquina/runner, nao faz parte do historico
whey-watch.log
*.tmp
summary.md

# o estado NAO esta aqui de proposito: e ele que carrega o historico de precos
# entre execucoes na nuvem, por isso e commitado a cada ronda

# estado local do wrangler: guarda em cache o id e o NOME da conta Cloudflare,
# e o nome e derivado do email de registo. nao pertence a um repo publico.
bot/.wrangler/
.wrangler/
